# 🚀 Бърз Старт: Anti-Spam Защита

## Какво Прави?

Защитава от:
- 🚫 Спам резервации (макс 3 за 24ч)
- ⚠️ Дублирани резервации (същ клиент в рамките на 7 дни)
- 🔍 Съмнително поведение (много резервации за кратко време)

## Как Работи?

### За Клиенти:
1. Нормална резервация → Записва се веднага ✅
2. Дублирана резервация → Предупреждение + изисква потвърждение ⚠️
3. Твърде много резервации → Блокира напълно 🚫

### За Администратори:
- Съмнителни резервации имат **жълт бордър** + badge "Съмнителна"
- Кликнете badge-а за детайли
- Препоръчва се обаждане на клиента за потвърждение

## Настройки

Файл: `/src/lib/antiSpam.ts`

```typescript
export const ANTI_SPAM_CONFIG = {
  maxBookingsPerPeriod: 3,      // Макс резервации
  periodHours: 24,              // За колко време
  minDaysBetweenBookings: 7,    // Минимални дни между резервации
  flagIfMoreThan: 2,            // Флагва след X резервации
  flagPeriodHours: 2,           // За колко време
};
```

## Променете Лимитите

### По-строга защита:
```typescript
maxBookingsPerPeriod: 2,
minDaysBetweenBookings: 14,
```

### По-либерална защита:
```typescript
maxBookingsPerPeriod: 5,
minDaysBetweenBookings: 3,
```

## Бързо Тестване

1. Отворете `http://localhost:3000`
2. Направете 2 резервации със **същото име и телефон** за близки дати
3. Трябва да видите **warning modal**
4. Отидете в `/admin` и проверете за **amber badge**

## Файлове

- `src/lib/antiSpam.ts` - Логика
- `src/components/booking/DuplicateWarning.tsx` - Modal
- `src/components/admin/AppointmentCard.tsx` - Admin визуализация
- `ANTI_SPAM_PROTECTION.md` - Пълна документация
- `TEST_ANTI_SPAM.md` - Тестови сценарии

## Troubleshooting

### Не виждам предупреждения?
- Уверете се че използвате **точно същото** име и телефон
- Проверете Console (F12) за грешки

### Искам да изтрия тестовите данни?
```javascript
// В Browser Console:
localStorage.removeItem('barbershop_appointments');
location.reload();
```

### Искам да деактивирам защитата временно?
В `antiSpam.ts`:
```typescript
maxBookingsPerPeriod: 999,
minDaysBetweenBookings: 0,
```

---

**За повече информация вижте `ANTI_SPAM_PROTECTION.md`**
