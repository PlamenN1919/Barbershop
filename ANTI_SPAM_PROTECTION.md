# 🛡️ Защита от Повторни Резервации и Спам

## Преглед

Системата има multi-layer защита, която предотвратява спам резервации и дублирани записвания, като същевременно дава гъвкавост на легитимните клиенти.

## Какво е Защитено?

### 1. **Rate Limiting (Ограничение на скоростта)**
- **Максимум 3 резервации за 24 часа** от един клиент
- При надвишаване: **пълна блокада** + съобщение за контакт с бръснарницата

### 2. **Duplicate Detection (Откриване на дубликати)**
- Проверка за **съществуваща резервация в рамките на 7 дни**
- Проверка за **точен дубликат** (същата дата и час)
- Идентификация по: **Име + Телефон** (нормализирани)

### 3. **Suspicious Behavior (Съмнително поведение)**
- Флагване при **2+ резервации за 2 часа**
- Флагване при **5+ активни резервации**
- Съмнителните резервации се маркират за admin преглед

## Как Работи?

### За Клиентите (Booking Form)

#### Сценарий 1: Нормална резервация ✅
```
Клиент прави резервация → Проверка минава → Резервацията се записва веднага
```

#### Сценарий 2: Дублирана резервация ⚠️
```
Клиент прави резервация → Открита дублирана/близка резервация →
Показва се предупреждение със:
  - Списък на съществуващите резервации
  - Причина за предупреждението
  - Опция за потвърждение или отказ
→ Клиент избира → Ако потвърди, резервацията се записва с флаг
```

#### Сценарий 3: Rate Limit блокада 🚫
```
Клиент прави 4-та резервация за 24ч → Блокиране →
Показва се съобщение: "Достигнахте лимита, моля свържете се директно"
→ Резервацията НЕ се записва
```

### За Администратори (Admin Panel)

#### Визуални Индикатори
- **Жълт/Amber бордър** около картата на резервацията
- **Badge "Съмнителна"** с ⚠️ икона
- **Кликабилен badge** показва детайли при клик:
  - Причина за флагване
  - Брой активни резервации
  - Препоръка за обаждане на клиента

#### При Ръчно Добавяне
Когато администратор добавя резервация през CalendarView:
1. Същите проверки като при booking form
2. При съмнително поведение → **Confirmation dialog**:
   ```
   ⚠️ ВНИМАНИЕ: Открита съмнителна резервация!
   
   Вече имате 2 активни резервации в рамките на 7 дни.
   
   Активни резервации от Иван Иванов: 3
   
   Сигурни ли сте, че искате да добавите тази резервация?
   ```
3. При потвърждение → Резервацията се записва с флаг

## Конфигурация

Настройките се намират в `/src/lib/antiSpam.ts`:

```typescript
export const ANTI_SPAM_CONFIG = {
  // Максимум резервации за определен период
  maxBookingsPerPeriod: 3,
  periodHours: 24,
  
  // Минимално време между резервации от същия клиент
  minDaysBetweenBookings: 7,
  
  // Флагове за съмнително поведение
  flagIfMoreThan: 2, // Резервации в период
  flagPeriodHours: 2, // За колко време
};
```

### Как да Промените Настройките?

**Пример 1: По-строга защита**
```typescript
maxBookingsPerPeriod: 2,  // Само 2 резервации
periodHours: 48,          // За 48 часа
minDaysBetweenBookings: 14, // 2 седмици между резервации
```

**Пример 2: По-либерална защита**
```typescript
maxBookingsPerPeriod: 5,  // 5 резервации
periodHours: 12,          // За 12 часа
minDaysBetweenBookings: 3, // 3 дни между резервации
```

## Типове Проверки

### 1. Exact Duplicate (Точен дубликат)
- Същото име + телефон
- Същата дата и час
- **Резултат**: Показва предупреждение, изисква потвърждение

### 2. Recent Booking (Близка резервация)
- Същото име + телефон
- Резервация в рамките на X дни (по подразбиране 7)
- **Резултат**: Показва предупреждение, изисква потвърждение

### 3. Rate Limit (Ограничение на скоростта)
- Същото име + телефон
- 3+ резервации за последните 24 часа
- **Резултат**: Пълна блокада, не позволява резервация

### 4. Suspicious Activity (Съмнителна активност)
- 2+ резервации за 2 часа → Флагва
- 5+ активни резервации → Флагва
- **Резултат**: Позволява резервацията, но я маркира за admin преглед

## Как Се Идентифицира Клиентът?

### Нормализация на Телефон
```typescript
"+359 88 123 4567"  → "359881234567"
"+359-88-123-4567"  → "359881234567"
"088 123 4567"      → "0881234567"
```

### Нормализация на Име
```typescript
"Иван  Иванов"     → "иван иванов"
"ИВАН ИВАНОВ"      → "иван иванов"
" Иван Иванов "    → "иван иванов"
```

Две резервации се считат от един клиент, ако:
- Нормализираните имена съвпадат **И**
- Нормализираните телефони съвпадат

## Admin Workflow (Работен процес)

### Преглед на Флагнати Резервации

1. **Откриване**: Резервацията има жълт бордър + badge "Съмнителна"
2. **Проверка**: Кликнете на badge-а за детайли
3. **Действие**:
   - Обадете се на клиента на посочения телефон
   - Потвърдете дали резервациите са легитимни
   - Ако е спам → Отменете/Изтрийте резервацията
   - Ако е легитимно → Оставете резервацията

### Примерно Известие за Admin

```
⚠️ СЪМНИТЕЛНА РЕЗЕРВАЦИЯ
Клиент: Иван Петров
Телефон: +359 88 123 4567
Активни резервации: 3
Причина: Вече имате 2 активни резервации в рамките на 7 дни.

💡 Препоръчваме да се свържете с клиента за потвърждение.
```

## Технически Детайли

### Файлове

- **`/src/lib/antiSpam.ts`** - Главна логика за проверки
- **`/src/components/booking/DuplicateWarning.tsx`** - Modal за предупреждения
- **`/src/components/admin/AppointmentCard.tsx`** - Визуализация на флагове
- **`/src/lib/types.ts`** - Добавени полета `isFlagged`, `flagReason`

### API Functions

```typescript
// Проверка за дублирана резервация
checkForDuplicateBooking(formData: BookingFormData): DuplicateCheckResult

// Трябва ли да блокираме напълно?
shouldBlockBooking(result: DuplicateCheckResult): boolean

// Трябва ли да изискаме потвърждение?
shouldRequireConfirmation(result: DuplicateCheckResult): boolean

// Генерира текст за admin известие
generateAdminNotification(formData, result): string | null
```

### Data Structure

```typescript
interface Appointment {
  // ... други полета
  isFlagged?: boolean;    // Дали е флагната
  flagReason?: string;     // Причина за флагване
}

interface DuplicateCheckResult {
  isDuplicate: boolean;          // Има ли дубликат?
  isSuspicious: boolean;         // Съмнително ли е?
  existingBookings: Appointment[]; // Съществуващи резервации
  warnings: string[];            // Предупреждения за показване
  reason?: string;               // Код на причина
}
```

## Тестване

### Тест 1: Нормална резервация
1. Направете резервация с ново име/телефон
2. ✅ Трябва да мине без предупреждения

### Тест 2: Дублирана резервация
1. Направете резервация
2. Опитайте да направите още една за същата дата/час
3. ⚠️ Трябва да покаже предупреждение
4. Потвърдете → Резервацията се записва с флаг

### Тест 3: Rate Limit
1. Направете 3 резервации за различни дати със същото име/телефон
2. Опитайте 4-та резервация в рамките на 24ч
3. 🚫 Трябва да блокира напълно

### Тест 4: Admin флагове
1. Направете съмнителна резервация (повече от 2 за 2 часа)
2. Отидете в admin panel
3. ⚠️ Резервацията трябва да е с жълт бордър + badge "Съмнителна"
4. Кликнете badge → Трябва да покаже детайли

## Често Задавани Въпроси

### Може ли клиент да има 2 резервации за различни дати?
Да, ако са на повече от 7 дни разлика. Ако са по-близо, ще се покаже предупреждение за потвърждение.

### Какво става ако клиент иска да резервира за себе си и приятел?
Клиентът трябва да използва различни имена и телефони за двете резервации. Ако използва едни и същи данни, системата ще покаже предупреждение.

### Администраторът може ли да заобиколи защитата?
Да, администраторът получава confirmation dialog, но може да потвърди резервацията дори при предупреждение.

### Как да изтрия флаг от резервация?
В момента няма UI за това, но можете да изтриете и пресъздадете резервацията, или да редактирате localStorage директно (не се препоръчва).

### Защитата работи ли offline?
Да, всички проверки работят с localStorage. При refresh на страницата данните се запазват.

## Performance (Производителност)

- ✅ Всички проверки са O(n) където n е броят резервации
- ✅ Проверките се извършват само при опит за резервация
- ✅ Няма забавяне на UI - проверките са синхронни
- ✅ localStorage е достатъчно бърз за стотици резервации

## Бъдещи Подобрения

- [ ] Email/SMS известия до admin при флагнати резервации
- [ ] Dashboard за статистики на спам опити
- [ ] Machine learning за по-интелигентно откриване на спам
- [ ] Whitelist/Blacklist на телефонни номера
- [ ] History на отменени резервации за по-добра защита
- [ ] Captcha при много опити за кратко време

---

**Забележка**: Тази защита е балансирана да предпази от спам, без да пречи на легитимни клиенти. При нужда можете лесно да промените конфигурацията в `antiSpam.ts`.
